name: Probar POKEMONSTOCK

on:
  push:
  workflow_dispatch:

jobs:
  probar:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar código
        uses: actions/checkout@v6

      - name: Instalar Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Instalar librerías
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Comprobar sintaxis
        run: |
          python -m py_compile app.py

      - name: Comprobar librerías
        run: |
          python -c "import requests; import bs4; print('Librerías OK')"
          
      - name: Comprobar conexión web
        run: |
          python -c "import requests; r=requests.get('https://www.pokemillon.com/', timeout=20); print('Pokemillon:', r.status_code)"
          python -c "import requests; r=requests.get('https://todohits.com/', timeout=20); print('TodoHits:', r.status_code)"
